services:
  - type: web
    name: isolarchecking-backend
    env: node
    plan: free
    rootDir: backend
    buildCommand: |
      pip install -r backend/requirements.txt
      yarn install
    startCommand: node index.js
    autoDeploy: false
